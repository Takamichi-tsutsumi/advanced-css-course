{
  "name": "natours",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "serve ./",
    "start:watch": "yarn run build:watch & yarn start",
    "build": "node-sass --include-path scss sass/main.scss ./css/style.css",
    "build:watch":
      "node-sass --include-path scss sass/main.scss ./css/style.css -w",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^4.7.2"
  },
  "dependencies": {
    "serve": "^6.5.3"
  }
}
